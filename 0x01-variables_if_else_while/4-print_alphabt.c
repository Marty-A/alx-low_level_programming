#include <stdio.h>
/**
 * main - Print alphadets with exceptions in lowercase
 * Return: 0
 */
int main(void)
{
	char y;

	for (y = 'a' ; y < 'e' ; y >= 'f')
	for (y < 'q' ; y >= 'r' ;)

	putchar(y);
	putchar('\n');

	return (0);
}
